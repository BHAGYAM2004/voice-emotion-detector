# Web framework
flask==3.0.0
gunicorn==22.0.0

# ML - Using ONNX Runtime (lightweight) + PyTorch for compatibility
# ONNX Runtime is ~50MB vs PyTorch ~300MB+
torch==2.10.0
torchaudio==2.10.0
transformers==4.46.0
optimum==1.26.0  # For ONNX model support and quantization
onnxruntime==1.24.1  # Lightweight inference engine (~50MB)

# Audio processing - Python 3.11 compatible with prebuilt wheels
librosa==0.10.2
soundfile==0.12.1
numpy==1.26.4
pandas==2.0.3

# Visualization
plotly==5.18.0

# Audio conversion
pydub==0.25.1
